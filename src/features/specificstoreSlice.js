import { createSlice } from '@reduxjs/toolkit'
const specificstoreSlice = createSlice({
  name: 'specificstore',
  initialState:{
      specificstoreList:[]
  },
  reducers: {}
});

export const {} = specificstoreSlice.actions

export default specificstoreSlice.reducer